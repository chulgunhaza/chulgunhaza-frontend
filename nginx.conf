server {
    listen 3000;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # React Router(클라이언트 사이드 라우팅) 대응 — 존재하지 않는 경로는 전부
    # index.html로 폴백시켜서 새로고침/직접 URL 진입 시 404가 안 나게 한다.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
